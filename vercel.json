{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "crons": [{ "path": "/api/internal/jobs/expire-reservations", "schedule": "*/5 * * * *" }]
}
